# 🌱 نظام إدارة مزرعة الخضروات

## 📋 نظرة عامة

نظام متكامل لإدارة مبيعات مزرعة الخضروات، يسمح لصاحب المزرعة بإدارة المنتجات والمحلات والحجوزات، بينما يمكن لأصحاب المحلات تصفح المنتجات وإنشاء الحجوزات.

## 🎯 الميزات الرئيسية

### 👨‍🌾 لوحة تحكم صاحب المزرعة (Admin)
- ✅ **إدارة المنتجات**: إضافة، تعديل، حذف المنتجات مع الأسعار والكميات
- ✅ **إدارة المحلات**: إنشاء حسابات للمحلات وتفعيل/تعطيل الحسابات
- ✅ **إدارة الحجوزات**: عرض جميع الحجوزات وتأكيدها أو إلغائها
- ✅ **الإحصائيات**: عرض إحصائيات شاملة (المنتجات، المحلات، المبيعات)
- ✅ **التقارير**: رسوم بيانية للمبيعات حسب المنتج
- ✅ **التنبيهات**: تنبيهات للمنتجات قاربت على النفاد

### 🏪 لوحة تحكم صاحب المحل (Shop)
- ✅ **تصفح المنتجات**: عرض جميع المنتجات المتاحة مع الأسعار والكميات
- ✅ **سلة التسوق**: إضافة المنتجات للسلة وتعديل الكميات
- ✅ **إنشاء الحجوزات**: إنشاء حجوزات جديدة مع خصم تلقائي من المخزون
- ✅ **عرض الحجوزات**: عرض جميع الحجوزات السابقة وحالتها
- ✅ **الملاحظات**: إضافة ملاحظات للحجوزات

### 🔐 نظام المصادقة
- ✅ تسجيل دخول آمن للمسؤولين والمحلات
- ✅ إدارة الجلسات باستخدام localStorage
- ✅ حماية المسارات حسب دور المستخدم

## 🌐 الروابط

- **رابط التطبيق**: https://3000-ic79hri1jdok5ze2z3n0e-d0b9e1e2.sandbox.novita.ai
- **المجلد**: `/home/user/webapp`

## 👥 الحسابات التجريبية

### حساب صاحب المزرعة (Admin)
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

### حسابات المحلات (Shops)
- **المحل 1**: `shop1` / `shop123`
- **المحل 2**: `shop2` / `shop123`
- **المحل 3**: `shop3` / `shop123`

## 🗄️ هيكل قاعدة البيانات

### جدول المستخدمين (users)
```sql
- id: معرف المستخدم
- username: اسم المستخدم (فريد)
- password: كلمة المرور
- name: الاسم الكامل
- role: الدور (admin أو shop)
- phone: رقم الهاتف
- address: العنوان
- status: الحالة (active أو inactive)
- created_at: تاريخ الإنشاء
```

### جدول المنتجات (products)
```sql
- id: معرف المنتج
- name_ar: الاسم بالعربي
- name_en: الاسم بالإنجليزي
- description: الوصف
- price: السعر (ريال عماني)
- unit: الوحدة (كرتون)
- quantity: الكمية المتاحة
- image_url: رابط الصورة
- status: الحالة (available أو unavailable)
- created_at: تاريخ الإضافة
- updated_at: تاريخ آخر تحديث
```

### جدول الحجوزات (orders)
```sql
- id: معرف الحجز
- shop_id: معرف المحل
- total_amount: المبلغ الإجمالي
- status: الحالة (pending، confirmed، cancelled)
- receipt_url: رابط الإيصال
- notes: ملاحظات
- created_at: تاريخ الحجز
- updated_at: تاريخ آخر تحديث
```

### جدول عناصر الحجز (order_items)
```sql
- id: المعرف
- order_id: معرف الحجز
- product_id: معرف المنتج
- product_name: اسم المنتج
- quantity: الكمية
- price: السعر
- total: الإجمالي
- created_at: تاريخ الإضافة
```

## 🛠️ التقنيات المستخدمة

### Backend
- **Hono**: إطار عمل خفيف وسريع للويب
- **Cloudflare Workers**: منصة الاستضافة على الحافة
- **Cloudflare D1**: قاعدة بيانات SQLite موزعة عالمياً

### Frontend
- **TailwindCSS**: إطار عمل CSS للتصميم السريع
- **Font Awesome**: الأيقونات
- **Chart.js**: الرسوم البيانية
- **Axios**: مكتبة HTTP للتواصل مع API

### DevOps
- **Wrangler**: أداة CLI لـ Cloudflare
- **PM2**: إدارة العمليات
- **Git**: إدارة الإصدارات
- **Vite**: أداة البناء

## 📂 هيكل المشروع

```
webapp/
├── src/
│   └── index.tsx          # التطبيق الرئيسي وAPI endpoints
├── public/
│   └── static/
│       └── app.js         # الواجهة الأمامية (Frontend)
├── migrations/
│   └── 0001_initial_schema.sql  # هيكل قاعدة البيانات
├── seed.sql               # البيانات التجريبية
├── wrangler.jsonc         # تكوين Cloudflare
├── ecosystem.config.cjs   # تكوين PM2
├── package.json           # التبعيات والسكربتات
└── README.md              # التوثيق
```

## 🚀 API Endpoints

### المصادقة
- `POST /api/auth/login` - تسجيل الدخول

### المنتجات
- `GET /api/products` - جلب جميع المنتجات
- `GET /api/products/:id` - جلب منتج واحد
- `POST /api/products` - إضافة منتج جديد (Admin)
- `PUT /api/products/:id` - تحديث منتج (Admin)
- `DELETE /api/products/:id` - حذف منتج (Admin)

### المحلات
- `GET /api/shops` - جلب جميع المحلات (Admin)
- `POST /api/shops` - إنشاء حساب محل (Admin)
- `PUT /api/shops/:id/status` - تحديث حالة المحل (Admin)

### الحجوزات
- `GET /api/orders` - جلب الحجوزات
- `GET /api/orders/:id` - جلب تفاصيل حجز
- `POST /api/orders` - إنشاء حجز جديد
- `PUT /api/orders/:id/status` - تحديث حالة الحجز (Admin)
- `PUT /api/orders/:id/receipt` - رفع إيصال الدفع

### الإحصائيات
- `GET /api/stats/dashboard` - إحصائيات لوحة التحكم
- `GET /api/stats/sales` - إحصائيات المبيعات

## 💻 الأوامر المتاحة

### التطوير
```bash
# بناء المشروع
npm run build

# تشغيل التطبيق محلياً
npm run dev:sandbox

# تطبيق migrations على قاعدة البيانات
npm run db:migrate:local

# إضافة البيانات التجريبية
npm run db:seed

# إعادة تعيين قاعدة البيانات
npm run db:reset

# تنظيف البورت
npm run clean-port
```

### النشر
```bash
# نشر للإنتاج
npm run deploy:prod

# تطبيق migrations على الإنتاج
npm run db:migrate:prod
```

## 📱 واجهة المستخدم

### التصميم
- ✅ **واجهة عربية كاملة** مع دعم RTL
- ✅ **تصميم متجاوب** يعمل على جميع الأجهزة (موبايل، تابلت، كمبيوتر)
- ✅ **ألوان زراعية** (أخضر، بني، أبيض)
- ✅ **أيقونات واضحة** من Font Awesome
- ✅ **إشعارات تفاعلية** لنجاح/فشل العمليات

### التجربة
- ✅ **تصفح سلس** بين الأقسام
- ✅ **نماذج منبثقة** للإضافة والتعديل
- ✅ **جداول منظمة** لعرض البيانات
- ✅ **بطاقات جذابة** للمنتجات
- ✅ **رسوم بيانية** تفاعلية

## 🔄 سير العمل

### 1. صاحب المزرعة
1. تسجيل الدخول بحساب Admin
2. إضافة المنتجات المتاحة مع الأسعار والكميات
3. إنشاء حسابات للمحلات
4. متابعة الحجوزات الواردة
5. تأكيد أو إلغاء الحجوزات
6. عرض التقارير والإحصائيات

### 2. صاحب المحل
1. تسجيل الدخول بحساب المحل
2. تصفح المنتجات المتاحة
3. إضافة المنتجات للسلة مع تحديد الكميات
4. تأكيد الحجز (يتم خصم الكميات تلقائياً)
5. متابعة حالة الحجوزات

### 3. إدارة المخزون
- ✅ خصم تلقائي من المخزون عند إنشاء الحجز
- ✅ إرجاع الكميات للمخزون عند إلغاء الحجز
- ✅ تنبيهات للمنتجات قاربت على النفاد (أقل من 20)

## 🎨 الألوان المستخدمة

- **الأخضر الرئيسي**: `#16a34a` (Green-600)
- **الأخضر الداكن**: `#15803d` (Green-700)
- **الأخضر الفاتح**: `#22c55e` (Green-500)
- **الخلفية**: `from-green-50 to-green-100`
- **النصوص**: `text-gray-800`, `text-gray-600`

## 📊 الرسوم البيانية

يتضمن التطبيق رسماً بيانياً تفاعلياً يعرض:
- مبيعات كل منتج بالريال العماني
- الكمية المباعة لكل منتج
- عدد الطلبات لكل منتج

## 🔒 الأمان

- ✅ التحقق من صحة المدخلات
- ✅ حماية المسارات حسب دور المستخدم
- ✅ تخزين كلمات المرور (يُنصح باستخدام bcrypt في الإنتاج)
- ✅ التحقق من توفر الكميات قبل الحجز

## 📝 ملاحظات مهمة

1. **العملة**: جميع الأسعار بالريال العماني (ر.ع)
2. **الوحدات**: الوحدة الافتراضية هي "كرتون" ويمكن تعديلها
3. **المخزون**: يتم خصم الكميات تلقائياً عند إنشاء الحجز
4. **البيانات التجريبية**: يتضمن التطبيق 8 منتجات و3 محلات للتجربة

## 🚧 التطوير المستقبلي

- [ ] رفع الإيصالات (صور) بعد الدفع
- [ ] نظام إشعارات فورية
- [ ] تقارير PDF قابلة للتحميل
- [ ] دعم اللغة الإنجليزية
- [ ] نظام المفضلة للمحلات
- [ ] تقييمات المنتجات
- [ ] تطبيق موبايل

## 📞 الدعم الفني

لأي استفسارات أو مشاكل، يرجى التواصل من خلال:
- فتح Issue على GitHub
- التواصل مع فريق التطوير

## 📄 الرخصة

هذا المشروع مفتوح المصدر ومتاح للاستخدام التجاري والشخصي.

---

**تم التطوير بـ ❤️ باستخدام Hono + Cloudflare Workers + TailwindCSS**

آخر تحديث: 2025-10-23
